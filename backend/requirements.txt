fastapi
uvicorn[standard]
neo4j>=5.0
openai
APScheduler>=3.10,<4.0
httpx
pydantic
pydantic-settings
pytest
pytest-asyncio
python-dotenv
# MCP server for public read-only graph access (ADR-0017, backend/mcp_server.py).
# Pinned <2: mcp 2.0 removed mcp.server.fastmcp (FastMCP is now mcp.server.mcpserver),
# so an unpinned rebuild crash-loops the backend on import. Lift the pin only together
# with porting backend/mcp_server.py to the 2.x API.
mcp>=1.2,<2
